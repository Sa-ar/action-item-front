import { Button } from "@mui/material";

const Homepage = () => {
  return (
    <div>
      <Button variant="contained" color="primary">
        Fetch
      </Button>
      <Button variant="contained" color="primary">
        History
      </Button>
    </div>
  );
};

export default Homepage;
